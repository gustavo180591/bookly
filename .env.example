# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bookly?schema=public"

# Admin
ADMIN_PASSWORD=your_secure_password
ADMIN_EMAIL=admin@example.com

# Security
CSRF_SECRET=generate_a_secure_random_string
SESSION_SECRET=generate_another_secure_random_string

# SMTP Configuration (required for email functionality)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=usuario
SMTP_PASS=clave
SMTP_FROM="Bookly <noreply@example.com>"

# App
ORIGIN=http://localhost:3001
